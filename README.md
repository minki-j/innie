To run the all components of the app with an AI coding assistant agent, pass .cursor/skills/run-services/SKILL.md to the agent and approve bash commands the agent generates. Don't forget to set up a redis and postgres DB in advance.
- application
- agents
- orchestrator server
- orchestrator prefect (optional)
- lab (optional)